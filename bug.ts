function printString(str: string): void {
  console.log(str);
}

function printNumber(num: number): void {
  console.log(num);
}

printString(123); // This will compile and run without error, but it's logically incorrect